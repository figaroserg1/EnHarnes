---
allowed-tools: Bash
description: Run full test suite (lint + structural tests)
---

Run `make test` and report results. If any check fails, show the error and suggest a fix.

Next: if all passed, suggest `/harness.review` for full pre-PR gate.
