#!/usr/bin/env bash
# =============================================================================
# obs-down.sh — Остановка локального observability-стека
# =============================================================================
#
# ЧТО ДЕЛАЕТ:
#   Заглушка (placeholder). В будущем будет останавливать контейнеры
#   observability-стека (логи, метрики, трейсы) — например,
#   docker compose -f observability.compose.yml down.
#
# ЗАЧЕМ НУЖЕН:
#   Парный скрипт к obs-up.sh. Нужен для корректного завершения
#   observability-сервисов после окончания работы, чтобы не оставлять
#   запущенные контейнеры.
#
# СТАТУС: placeholder — требует реализации (Phase 2/3 obs).
# =============================================================================
set -euo pipefail

echo "TODO: [AI] Остановить локальный observability stack"
echo "EXAMPLE (REPLACE ME): docker compose -f observability.compose.yml down"
