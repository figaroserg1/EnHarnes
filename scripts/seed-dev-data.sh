#!/usr/bin/env bash
# =============================================================================
# seed-dev-data.sh — Наполнение локальной БД тестовыми данными
# =============================================================================
#
# ЧТО ДЕЛАЕТ:
#   Заглушка (placeholder). В будущем будет загружать фикстуры (тестовые
#   данные) в локальную базу данных для разработки: пользователи, проекты,
#   примеры транзакций и т.д.
#
# ЗАЧЕМ НУЖЕН:
#   Без seed-данных локальная среда пуста и бесполезна для разработки.
#   Этот скрипт обеспечивает воспроизводимое начальное состояние БД,
#   чтобы каждый разработчик и агент работал с одинаковым набором данных.
#   Используется в Makefile: `make seed`.
#
# СТАТУС: placeholder — требует реализации человеком.
# =============================================================================
set -euo pipefail

echo "TODO: [HUMAN] Добавить сидирование dev-данных"
echo "EXAMPLE (REPLACE ME): ./scripts/seed-dev-data.sh --fixture minimal"
