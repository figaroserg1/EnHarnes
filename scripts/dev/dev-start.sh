#!/usr/bin/env bash
# =============================================================================
# dev-start.sh — Запуск локального dev-окружения приложения
# =============================================================================
#
# ЧТО ДЕЛАЕТ:
#   Заглушка (placeholder). В будущем будет поднимать все сервисы, необходимые
#   для локальной разработки: приложение, базу данных, очереди и т.д.
#
# ЗАЧЕМ НУЖЕН:
#   Единая точка входа для запуска dev-окружения. Вместо того чтобы помнить
#   docker compose команды или набор флагов, разработчик запускает один скрипт.
#   Используется в Makefile: `make dev`.
#
# СТАТУС: placeholder — требует реализации человеком.
# =============================================================================
set -euo pipefail

echo "TODO: [HUMAN] Заменить на запуск реального приложения"
echo "EXAMPLE (REPLACE ME): docker compose up app db"
